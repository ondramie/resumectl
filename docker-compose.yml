services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: resumectl
      POSTGRES_PASSWORD: resumectl
      POSTGRES_DB: resumectl
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
